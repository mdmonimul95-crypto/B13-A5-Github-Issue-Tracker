1 { (var can be redeclared and reassigned)
  (let cannot be redeclared but can be reassigned)
  (const cannot be redeclared or reassigned)}


2 {Spread operator used to expand elements of an array or object.}

3 { (map returns a new array after modifying elements)

   (filter returns a new array based on a condition)

   (forEach loops through array but does not return a new array)}

4{Arrow function is a short syntax to write functions in JavaScript.}

5{Template literals is used to insert variables inside a string using backticks}